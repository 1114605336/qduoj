#include <stdio.h>

int main(int argc, char *argv[]) {
	int n;
	scanf("%d",&n);
	double a[n];
	for (int i=0;i<n;i++) {
		scanf("%lf",&a[i]);
	}
	double sum=0;
	for (int i=0;i<n;i++) {
		sum+=(1.0/a[i]);
	}
	printf("%.3lf\n",1.0/sum);
}
