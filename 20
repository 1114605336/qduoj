#include<stdio.h>
int main()
{
	int num = 0;
	int cnt = 1;
	int tmp;
	int n;
	scanf("%d",&n);
	while(n--)
	{
		scanf("%d",&tmp);
		if(cnt==0)
		{
			num = tmp;
			cnt = 1;
		}
		else 
		{
			if(num!=tmp)
				 cnt--;
			else
				 cnt++;
		 }
	 }
	 printf("%d",num);
	 return 0;
}
