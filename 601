#include <stdio.h>
int main(){
	int n,i,j;
	while(scanf("%d",&n)!=EOF){   //这里一定要有!=EOF，否则出 runtime error
		for(i=1;i<=n;i++)
			{
			for(j=1;j<=n-i;j++)
			printf(" ");	
			for(j=1;j<=2*i-1;j++)
			printf("*");	
			printf("\n");
			} 
	}
}
